procedure c12 is
	procedure A( r, s ) is
		integer x;
		integer y;
		x := r[0];
		y := s[0];
		out(x);
		out(y);
	end
begin
	array x;
	x := new integer[1];
	x[0] := 5;
	begin A(x, x);
end
